/*
 * Map of common names to PDG codes.
 * This is handwritten, as no suitable map could be found.
 */

let namePDGMap = Belt.Map.String.fromArray([
  // Electron
  ("e", 11),
  ("electron", 11),
  // Electron Neutrino
  ("nue", 12),
  ("electron neutrino", 12),
])
